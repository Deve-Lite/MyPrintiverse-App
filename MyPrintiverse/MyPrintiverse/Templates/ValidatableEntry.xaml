<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="MyPrintiverse.Templates.ValidatableEntry"
             x:Name="this">
    <VerticalStackLayout BindingContext="{x:Reference this}" Margin="{Binding Margin}"  Padding="{Binding Padding}" Spacing="0">
        <Label Style="{Binding TitleStyle}" Text="{Binding Title}"/>
        <Entry Style="{Binding EntryStyle}" Text="{Binding Text, Mode=TwoWay}" Placeholder="{Binding Placeholder}" IsPassword="{Binding IsPassword}" >
            <Entry.Behaviors>
                <toolkit:EventToCommandBehavior EventName="Unfocused" Command="{Binding EntryValidationCommand}"/>
            </Entry.Behaviors>
        </Entry>
        <Label Style="{Binding ErrorMessageStyle}" Text="{Binding ErrorMessage}" HorizontalOptions="Center"/>
    </VerticalStackLayout>
</ContentView>
